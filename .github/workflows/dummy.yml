name: Dummy CI Failure

on:
  pull_request:
    branches: ["main"]

jobs:
  dummy-check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Create sample artifact files
        run: |
          echo "test failure" > failure.txt
          echo "sample log file" > log.txt
      - name: Upload sample artifact
        # if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sample-artifact
          path: |
            failure.txt
            log.txt
      - name: Fail the workflow
        run: |
          echo "This is a dummy CI failure"
          exit 1
